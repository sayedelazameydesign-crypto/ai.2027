# Prompt versions

Prompts are versioned by directory. Add new behavior under a new version such as `v1/` or `v2/`; do not silently edit a version already referenced by an evaluation or production workflow.

Each version should include its prompt files and, when behavior changes, a corresponding evaluation fixture or expected-output note.
